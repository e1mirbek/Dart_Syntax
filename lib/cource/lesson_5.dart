/// [Anonymous] functions - (анонимные функций)

void main() {
  var list = ["apples", "bananas", "oranges"];
  for (var element in list) {
    print(element);
  }
  for (var element in list) {
    print(element);
  }
}
